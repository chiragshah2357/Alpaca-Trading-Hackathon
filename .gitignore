# Secrets — never commit
.env
.env.*
!.env.example
*.key
secrets.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Local data / logs (thesis log committed explicitly if desired)
*.log
*.sqlite
*.db

# Runtime agent state — created locally, force-committed only by the cron workflow
state/*.json
state/*.jsonl

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
